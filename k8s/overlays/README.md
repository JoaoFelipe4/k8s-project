# Environment Overlays

This directory holds environment-specific configurations. Using Kustomize, it patches the `base/` manifests to adapt resource limits, replicas, and specific environment variables for development, staging, or production.
