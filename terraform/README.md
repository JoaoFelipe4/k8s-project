# Infrastructure as Code

This directory contains Terraform configurations used to provision the underlying infrastructure.

## Responsibilities

- Define compute resources (e.g., Kubernetes clusters, node groups).
- Configure networking components (e.g., VPCs, subnets, load balancers).
- Manage identity and access (e.g., IAM roles).
