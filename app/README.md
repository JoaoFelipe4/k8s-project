# Application Source Code

This directory contains the source code for the microservices deployed in this project. Each subdirectory represents a standalone service and includes its own build configuration (e.g., Dockerfile) and dependencies.

## Directory Structure

- `backend/`: API services and data processing logic.
- `frontend/`: User interface and client-side applications.
